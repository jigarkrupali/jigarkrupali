import { Box, Typography } from "@mui/material";

export const Invitation = () => {
  return (
    <Box
      sx={{
        background: "rgba(255, 255, 255, 0.2)",
        boxShadow: "0px 4px 30px rgba(0, 0, 0, 0.3)",
        backdropFilter: "blur(4px)",
        border: "1px solid rgba(255, 255, 255, 0.2)",
        borderRadius: 4,
        marginX: 2,
        padding: 2,
      }}
    >
      <Typography
        variant="h4"
        color="#734967"
        fontFamily="Great Vibes"
        paddingLeft={2}
      >
        Please join us and be the part of happiest moment of our life as we tie
        the knot and become one in the presence of our loved ones.
      </Typography>
    </Box>
  );
};
<strong></strong>;
